1. npm install in backend and frontend
2. cd backend
    - npx hardhat node (in new terminal)
    - npx hardhat compile (in new terminal)
    - npx hardhat run scripts/deploy.js --network localhost (in new terminal)
    - node server/server.js (in new terminal)
3. cd frontend 
    - npm start (in new terminal)